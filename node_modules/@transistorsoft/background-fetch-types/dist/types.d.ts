/**
 * Authorization status of the Background Fetch API.
 *
 * | Value | Constant | Description |
 * |---|---|---|
 * | `0` | [[BackgroundFetch.STATUS_RESTRICTED]] | Background fetch updates are unavailable and the user cannot enable them again. For example, this status can occur when parental controls are in effect for the current user. |
 * | `1` | [[BackgroundFetch.STATUS_DENIED]] | The user explicitly disabled background behavior for this app or for the whole system. |
 * | `2` | [[BackgroundFetch.STATUS_AVAILABLE]] | Background fetch is available and enabled. |
 *
 * @example
 * ```ts
 * const status = await BackgroundFetch.status();
 * if (status === BackgroundFetch.STATUS_RESTRICTED) {
 *   console.log('Background fetch is restricted by the OS or parental controls.');
 * } else if (status === BackgroundFetch.STATUS_DENIED) {
 *   console.log('The user has disabled background fetch for this app.');
 * } else if (status === BackgroundFetch.STATUS_AVAILABLE) {
 *   console.log('Background fetch is available.');
 * }
 * ```
 */
export declare type BackgroundFetchStatus = 0 | 1 | 2;
/**
 * [Android only] Network type constraint for scheduled tasks.
 *
 * Used with [[AbstractConfig.requiredNetworkType]] on both [[BackgroundFetchConfig]]
 * and [[TaskConfig]].
 *
 * | Value | Constant | Description |
 * |---|---|---|
 * | `0` | [[BackgroundFetch.NETWORK_TYPE_NONE]] | No network constraint.  The job will run regardless of network state. |
 * | `1` | [[BackgroundFetch.NETWORK_TYPE_ANY]] | The job requires any active network connection. |
 * | `2` | [[BackgroundFetch.NETWORK_TYPE_UNMETERED]] | The job requires an unmetered (e.g. Wi-Fi) network connection. |
 * | `3` | [[BackgroundFetch.NETWORK_TYPE_NOT_ROAMING]] | The job requires a non-roaming network connection. |
 * | `4` | [[BackgroundFetch.NETWORK_TYPE_CELLULAR]] | The job requires a cellular (mobile data) network connection. |
 */
export declare type NetworkType = 0 | 1 | 2 | 3 | 4;
//# sourceMappingURL=types.d.ts.map